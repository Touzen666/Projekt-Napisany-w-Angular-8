version: '3.7'
# To be run only development issues
services:
  mysql:
    volumes:
      - mysql:/var/lib/mysql
    ports:
      - "3306:3306"
    expose:
      - "3306"
    image: touzen666/projekt8/mysql
    build:
      context: dockerfiles/mysql
      dockerfile: .
    networks:
      - backend
  backend:
    image: touzen666/projekt8/backend
    build:
      context: .
      dockerfile: Dockerfile
      target: backend
    expose:
      - "80"
    links:
      - mysql
    networks:
      - backend
  nginx:
    image: touzen666/projekt8/nginx
    build:
      context: dockerfiles/nginx
      dockerfile: dockerfiles/Dockerfile
    ports:
      - "80:80"
    links:
      - backend
    networks:
      - backend
      - bridge
volumes:
  mysql:
networks:
  backend:
    internal: true
  bridge:
    internal: false

